{
  "name": "aitokensharing-mock-server",
  "version": "1.1.0",
  "description": "AITokenSharing 模拟网关环境 — 用于验证产品分析中的 API 调用示例",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node tests/test-gateway.js && node tests/test-management.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "uuid": "^10.0.0"
  }
}
